# FIFA4ALL core dependencies (headless-friendly; used for cloud dev + CI)
#
# Pin 0.10.21: it exposes mp.solutions.face_mesh and is known to stay up on
# this Mac. Newer 0.10.3x wheels drop solutions; 1.x FaceLandmarker SIGABRTs
# (DrishtiMetalHelper). mediapipe brings a matching opencv build.
mediapipe==0.10.21
numpy>=1.24
pytest>=8
