# Optional webcam diagnostic/runtime dependencies for Person 1 tracking.
# Do not install these for simulation-only tests or CI unless webcam tracking
# is being exercised. The importable geometry and synthetic fixtures use only
# the Python standard library.
opencv-python>=4.9
mediapipe>=0.10
numpy>=1.26
